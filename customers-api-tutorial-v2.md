# Manage customer data with the Customers API

A hands-on tutorial that walks through a complete customer lifecycle using create, retrieve, update, and delete operations on a single resource.

[Read tutorial](#overview) · [Back to selected work](index.html#samples)

> REST API · CRUD workflow · cURL + JavaScript · Expanded sample

## What this tutorial demonstrates

- **A full CRUD journey on one resource.** The reader follows a single customer record from creation to deletion, reducing cognitive load.
- **Parallel examples in cURL and JavaScript.** Developers can switch between code styles without losing context or task continuity.
- **Explicit preparation, reference, and workflow.** Setup, resource model, and step-by-step tasks make the tutorial reusable as onboarding material.

## Overview

This tutorial is structured as a compact but complete onboarding flow for a fictional resource-oriented API.

> Tutorial sample · Fictional REST API

This tutorial demonstrates an end-to-end workflow for managing customer data with a fictional Customers API. The API, URLs, and example data are provided to illustrate structure, flow, and documentation style.

By the end of the tutorial, you will have created a customer record, retrieved it by ID, updated selected fields, and deleted the resource.

## Before you start

The setup section establishes the environment, credentials, and request tooling needed before the CRUD flow begins.

### Requirements and setup

> Authentication · Environment · Request tooling

Before you begin, make sure you have:

- An API access token with permission to call the Customers API.
- The base URL for your environment, such as sandbox or production.
- A tool for sending HTTP requests, such as `curl` or Postman.
- Network access to the `/api/1.0/customers` endpoint.

In the examples below, replace `{base_url}` and `{access_token}` with your own values. In a real application, you would typically store them in environment variables such as `BASE_URL` and `ACCESS_TOKEN`.

For safety, use a sandbox environment and a non-production token while testing this workflow.

### Base URL and headers

> Authentication and request format

All examples in this tutorial use bearer token authentication and JSON request bodies where applicable.

```http
Authorization: Bearer {access_token}
Content-Type: application/json
```

Include the `Authorization` header in every request. Include `Content-Type: application/json` when the request contains a JSON body, such as `POST` or `PATCH`.

In a real implementation, handle HTTP status codes such as `201 Created` or `4xx` errors explicitly and log or surface error messages to make debugging easier.

## Resource model

The tutorial introduces the resource model before the task sequence so the later examples stay predictable and easy to follow.

### Customer resource

> Field reference

The tutorial uses a single `Customer` resource with the following fields:

| Field | Type | Required | Description |
| :-- | :-- | :-- | :-- |
| `id` | string (UUID) | No | Stable unique identifier generated by the API. |
| `full_name` | string | Yes | Customer’s full name. |
| `email` | string | Yes | Customer email address. Typically unique per customer. |
| `business_name` | string | No | Optional company or organization name. |
| `created_at` | string (ISO 8601) | No | Timestamp when the record was created. |
| `updated_at` | string (ISO 8601) | No | Timestamp when the record was last updated. |

### Customers API snapshot

> Endpoint overview

- `POST /api/1.0/customers` — Create a new customer.
- `GET /api/1.0/customers/{id}` — Retrieve a customer by ID.
- `PATCH /api/1.0/customers/{id}` — Update selected fields on a customer.
- `DELETE /api/1.0/customers/{id}` — Delete a customer.

The tutorial uses a partial update model for `PATCH`: only the fields included in the request body are changed.

## CRUD tutorial

Each step keeps the task sequence explicit and lets the reader switch between cURL and JavaScript without losing context.

### 1. Create a customer

`POST /api/1.0/customers`

Create the customer record that you will use throughout the rest of this tutorial. The response returns its generated `id`, which you need for the retrieve, update, and delete requests.

#### cURL - Create customer

```bash
curl --request POST \
  --url {base_url}/api/1.0/customers \
  --header "Authorization: Bearer {access_token}" \
  --header "Content-Type: application/json" \
  --data '{
    "full_name": "John Doe",
    "email": "john@doe.com",
    "business_name": "Northwind Studio"
  }'
```

#### JavaScript - Create customer

```javascript
const response = await fetch(`${baseUrl}/api/1.0/customers`, {
  method: "POST",
  headers: {
    Authorization: `Bearer ${accessToken}`,
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    full_name: "John Doe",
    email: "john@doe.com",
    business_name: "Northwind Studio"
  })
});

if (!response.ok) {
  // Handle non-2xx responses in a real implementation
}

const customer = await response.json();
```

The client creates the initial customer record and obtains an `id` that you reuse in later requests.

### 2. Retrieve the customer

`GET /api/1.0/customers/{id}`

Retrieve the customer that you created in the previous step to confirm that it exists and to inspect the data returned by the API.

#### cURL - Retrieve customer

```bash
curl --request GET \
  --url {base_url}/api/1.0/customers/{id} \
  --header "Authorization: Bearer {access_token}"
```

#### JavaScript - Retrieve customer

```javascript
const response = await fetch(`${baseUrl}/api/1.0/customers/${customer.id}`, {
  method: "GET",
  headers: {
    Authorization: `Bearer ${accessToken}`
  }
});

if (!response.ok) {
  // Handle non-2xx responses in a real implementation
}

const customerRecord = await response.json();
```

Use this step to confirm that the resource was created successfully and that the API returns the expected fields.

### 3. Update the customer

`PATCH /api/1.0/customers/{id}`

Update selected fields on the existing customer without replacing the entire resource. This request changes the customer's name and business name while preserving fields that are not included in the request body.

#### cURL - Update customer

```bash
curl --request PATCH \
  --url {base_url}/api/1.0/customers/{id} \
  --header "Authorization: Bearer {access_token}" \
  --header "Content-Type: application/json" \
  --data '{
    "business_name": "Northwind Labs",
    "full_name": "John A. Doe"
  }'
```

#### JavaScript - Update customer

```javascript
const response = await fetch(`${baseUrl}/api/1.0/customers/${customer.id}`, {
  method: "PATCH",
  headers: {
    Authorization: `Bearer ${accessToken}`,
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    business_name: "Northwind Labs",
    full_name: "John A. Doe"
  })
});

if (!response.ok) {
  // Handle non-2xx responses in a real implementation
}

const updatedCustomer = await response.json();
```

The partial update modifies only the fields included in the request body. Other fields remain unchanged.

### 4. Delete the customer

`DELETE /api/1.0/customers/{id}`

Delete the test customer to complete the resource lifecycle and leave your environment in a clean state.

#### cURL - Delete customer

```bash
curl --request DELETE \
  --url {base_url}/api/1.0/customers/{id} \
  --header "Authorization: Bearer {access_token}"
```

#### JavaScript - Delete customer

```javascript
const response = await fetch(`${baseUrl}/api/1.0/customers/${customer.id}`, {
  method: "DELETE",
  headers: {
    Authorization: `Bearer ${accessToken}`
  }
});

if (!response.ok) {
  // Handle non-2xx responses in a real implementation
}
```

After the `DELETE` request succeeds, the API removes the customer record and typically returns `204 No Content`. A later `GET` request for the same `id` should return `404 Not Found`.

## Expected workflow result

At the end of this tutorial, you should have completed the following sequence on a single customer resource:

1. Create a customer record.
2. Retrieve the record by ID.
3. Update selected fields on the record.
4. Delete the record when testing is complete.

This structure helps new developers understand a complete resource lifecycle before they work with more complex endpoints or multi-resource workflows.

## Why this tutorial structure works

The sample is intentionally sequenced to reduce cognitive load for first-time readers while preserving technical accuracy.

### One resource, one journey

A single resource model keeps the workflow easy to follow and avoids context switching between unrelated examples.

### Reference-friendly sections

Each step can be reused later as standalone guidance without losing the logic of the full tutorial sequence.

### Fast path to confidence

The reader completes one meaningful task loop quickly, which makes the broader API feel easier to approach.

[Back to selected work](index.html#samples)
